"""Very Thoughtful Compression — codec-aware, quality-density video re-encoder.

The engine is UI-agnostic: the CLI (`vtc.cli`) and the future PySide6 GUI both
drive the same modules. See docs/quality-model.md for the bitrate/quality model.
"""

__version__ = "0.1.0"
